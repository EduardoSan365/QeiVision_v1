# QeiVision-API

API independiente de QeiVision. Consulta AutoShop y expone sólo los datos
necesarios para la consola web. Las credenciales se configuran exclusivamente
mediante variables de entorno.

Endpoints iniciales: `GET /health`, `GET /api/tiendas`, `GET /api/accesos` y
`GET /api/auditoria`.
